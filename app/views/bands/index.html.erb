<p id="notice"><%= notice %></p>
<nav class="navbar navbar-dark bg-dark">
<h1 class="text-center"><%= @bands.first.name %></h1>
<h3 class="text-center"><%= @bands.first.description  + ' ' + @bands.first.city %></h2>
</nav>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  </tbody>
</table>

<br>

